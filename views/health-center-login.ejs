<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Centro Médico | SangreVital</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts - Montserrat -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --sangre-primary: #dc3545;
            --sangre-secondary: #f8d7da;
            --sangre-dark: #c82333;
            --sangre-light: #f8f9fa;
            --sangre-card-shadow: 0 4px 20px rgba(220, 53, 69, 0.1);
        }
        
        body {
            background-color: #fefefe;
            font-family: 'Montserrat', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-size: 300px;
            background-attachment: fixed;
            background-color: rgba(255,255,255,0.9);
            background-blend-mode: overlay;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .login-container {
            max-width: 420px;
            width: 100%;
            margin: 0 auto;
        }

        .login-card {
            border: none;
            border-radius: 16px;
            box-shadow: var(--sangre-card-shadow);
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .login-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(220, 53, 69, 0.15);
        }

        .card-header {
            background: linear-gradient(135deg, var(--sangre-primary) 0%, var(--sangre-dark) 100%);
            color: white;
            padding: 1.5rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .card-header::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 100%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 70%);
        }

        .login-title {
            font-weight: 600;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
        }

        .form-label {
            font-weight: 500;
            color: #555;
        }

        .form-control {
            border-radius: 8px;
            padding: 0.75rem 1rem;
            border: 1px solid #ddd;
            transition: all 0.3s;
        }

        .form-control:focus {
            border-color: var(--sangre-primary);
            box-shadow: 0 0 0 0.25rem rgba(220, 53, 69, 0.25);
        }

        .btn-sangre {
            background-color: var(--sangre-primary);
            color: white;
            font-weight: 500;
            transition: all 0.3s;
            border: none;
            padding: 0.75rem;
            border-radius: 8px;
            box-shadow: 0 2px 10px rgba(220, 53, 69, 0.3);
        }

        .btn-sangre:hover {
            background-color: var(--sangre-dark);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(220, 53, 69, 0.4);
        }

        .alert-custom {
            background: rgba(220, 53, 69, 0.08);
            border-left: 4px solid var(--sangre-primary);
            border-radius: 8px;
            color: #333;
        }

        .login-footer {
            text-align: center;
            margin-top: 1.5rem;
            color: #666;
        }

        .login-footer a {
            color: var(--sangre-primary);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
        }

        .login-footer a:hover {
            color: var(--sangre-dark);
            text-decoration: underline;
        }

        /* Ajustes específicos para Montserrat */
        h1, h2, h3, h4, h5, h6 {
            font-weight: 600;
            letter-spacing: -0.5px;
        }

        /* Responsive adjustments */
        @media (max-width: 576px) {
            .login-container {
                padding: 0 1rem;
            }
            
            .login-card {
                border-radius: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <div class="card-header">
                <h2 class="login-title">
                    <i class="bi bi-hospital"></i> Acceso Centros Médicos
                </h2>
            </div>
            <div class="card-body p-4">
                <form action="/health-center/login" method="POST">
                    <div class="mb-4">
                        <label for="tax_id" class="form-label">NIT del Centro Médico</label>
                        <input type="text" class="form-control" id="tax_id" name="tax_id" required autofocus>
                    </div>
                    
                    <% if (typeof error !== 'undefined') { %>
                        <div class="alert alert-custom mb-4">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i> <%= error %>
                        </div>
                    <% } %>
                    
                    <button type="submit" class="btn btn-sangre w-100">
                        <i class="bi bi-box-arrow-in-right me-2"></i> Ingresar
                    </button>
                </form>
                
                <div class="login-footer">
                    <a href="/">
                        <i class="bi bi-arrow-left me-1"></i> Volver al inicio
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>